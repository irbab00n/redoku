/* Use subdirectories of this directory to represent the state control for views on your application */
import * as mainViewActions from './main-view-actions';

const actions = Object.assign(
  {},
  mainViewActions
);

export default actions;
